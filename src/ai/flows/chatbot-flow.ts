'use server';
/**
 * @fileOverview This flow is deprecated.
 * The application is now using the embeddable Vertex AI Search widget,
 * which handles its own API communication. The main logic is in `src/app/page.tsx`.
 */

// This file is intentionally left empty as its functionality has been replaced.
export {};
